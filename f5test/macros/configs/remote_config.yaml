%YAML 1.1
---
$extends:
    - static.yaml

header: |
    # Autogenerated by conf_gen %(VERSION)s
    #

footer: |
    # END

options:
    put users on Common partition: no

includes:
# "tmsh" here is a keyword that is associated with the "can_tmsh" flag.
    tmsh: remote_config.tmsh.yaml
    default: remote_config.default.yaml

# vim: ft=yaml
